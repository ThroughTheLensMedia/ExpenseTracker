import React from 'react';

export default function Invoice() {
    return (
        <section className="card">
            <h2>Invoice & CRM (Photography Clients)</h2>
            <div className="muted">
                Phase 3—client directory + projects/jobs + invoices + income posting.
            </div>
        </section>
    );
}
